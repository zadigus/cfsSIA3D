PROJECT(XMLParser)

INCLUDE_DIRECTORIES(
	${CMAKE_CURRENT_SOURCE_DIR}
	${CMAKE_CURRENT_BINARY_DIR}
	${XERCES_INCLUDE_DIRS}
)

FILE(GLOB src RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} "*.cpp" "*.hpp")
ADD_LIBRARY(XMLParser ${src})
TARGET_LINK_LIBRARIES(XMLParser ${XERCES_LIBRARIES})