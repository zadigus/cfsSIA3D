PROJECT(Utility)

ADD_SUBDIRECTORY(Logger)

INCLUDE_DIRECTORIES(
	${CMAKE_CURRENT_SOURCE_DIR}
	${CMAKE_CURRENT_BIN_DIR}
)

FILE(GLOB src RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} "*.cpp" "*.hpp")

ADD_LIBRARY(Utility ${src})
TARGET_LINK_LIBRARIES(Utility Logger)